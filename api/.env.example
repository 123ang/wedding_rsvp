# MySQL (create database `wedding_rsvp` and import schema/data before starting)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=wedding_rsvp

# Must match nginx (sites-available jsang-psong-wedding.com → localhost:3002)
PORT=3002
NODE_ENV=production
